# Copy to .env.local for local development and restart the dev server after changes.
# Production/preview server secrets belong in the target runtime's secret store;
# never expose privileged values through VITE_* variables.

# ─── Supabase (required) ───────────────────────────────────────────────────────
# TitanOS is Vite (not Next.js). Browser-visible values use VITE_*.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# Legacy JWT anon key OR new sb_publishable_… key both work:
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# Optional alias (same value as above):
# VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Server-only Supabase values — never VITE_*.
# SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SUPABASE_ACCESS_TOKEN=sbp_...   # optional; enables maintenance scripts when needed

# Optional AI / email providers (server-only)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# RESEND_API_KEY=re_...
# RESEND_FROM=TitanOS <noreply@yourdomain.com>

# Optional external job search provider (server-only; never VITE_*).
# Titan-native matching works without these. External results are queried only
# after the signed-in user explicitly enables outside-job search.
# ADZUNA_APP_ID=
# ADZUNA_APP_KEY=

# Public origin. Browser API calls are same-origin by default.
# Packaged native builds must set VITE_API_BASE_URL explicitly to the final
# production HTTPS origin after the Cloudflare migration is certified.
# VITE_TITANOS_PUBLIC_ORIGIN=https://titanos.app
# VITE_API_BASE_URL=https://titanos.app
# APP_ORIGIN=https://titanos.app

# Google OAuth: enable in Supabase Dashboard (see GOOGLE_AUTH.md).
# Never put Google Client Secret in VITE_* vars — only in Supabase Auth settings.

# Optional Stripe — server-only unless a variable is explicitly a public Vite flag.
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Server-only Stripe Price for the $9 Titan Autopilot Invoice Recovery Sprint.
# STRIPE_AUTOPILOT_PRICE_ID=price_...
# VITE_AUTOPILOT_ONETIME_CHECKOUT=false
# STRIPE_PRICE_STARTER_MONTHLY=price_...
# STRIPE_PRICE_PRO_MONTHLY=price_...
# STRIPE_PRICE_BUSINESS_MONTHLY=price_...
# Machine Payments Protocol (MPP) — /api/functions/mppPaid
# STRIPE_PROFILE_ID=profile_...
# MPP_SECRET_KEY=  # openssl rand -base64 32
# MPP_TESTNET=1
# MPP_TEMPO_AMOUNT=0.01
# MPP_STRIPE_AMOUNT=0.50
# MPP_STRICT_CACHE=0

# Optional: require email confirmation on /api/register (no auto-session).
# Secure default is true unless explicitly set to false.
# REGISTER_REQUIRE_EMAIL_CONFIRM=true

# Portal OTP pepper (server-only — prefer dedicated random string, not service role)
# PORTAL_OTP_PEPPER=long-random-string
# Legacy OTP/token compatibility is disabled by default. Only enable temporarily
# during a controlled migration window, then remove/disable again.
# PORTAL_OTP_ALLOW_LEGACY=0
# PORTAL_TOKEN_ALLOW_LEGACY=0

# Deep health (?deep=1) — header x-titanos-ops only
# HEALTH_DEEP_SECRET=long-random-string
# TITANOS_OPS_SECRET=long-random-string

# Optional Sentry observability.
# Browser DSN is public-by-design configuration; server DSN remains server-side.
# VITE_SENTRY_DSN=https://YOUR_KEY@o0.ingest.sentry.io/0
# VITE_SENTRY_REPLAY=0
# SENTRY_DSN=https://YOUR_KEY@o0.ingest.sentry.io/0
# SENTRY_ENVIRONMENT=production
# SENTRY_RELEASE=
# The old public /api/functions/sentryDebug fault-injection route is retired.
# Do not add SENTRY_DEBUG_ROUTE or Node-native CPU profiler flags to Workers.

# Optional first-party analytics controls
# ANALYTICS_INGEST_ENABLED=0
# VITE_ANALYTICS_INGEST=0

# Optional operational alert target (server-only)
# OPS_ALERT_WEBHOOK_URL=
# SLACK_WEBHOOK_URL=

# Optional TitanCom TURN (comma-separated URLs). Improves PTT through hard NATs.
# VITE_TURN_URLS=turn:turn.example.com:3478,turns:turn.example.com:5349
# VITE_TURN_USERNAME=
# VITE_TURN_CREDENTIAL=
