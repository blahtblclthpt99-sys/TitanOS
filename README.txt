Place your signed Android APK here as TitanOS.apk

The in-app "Download App" button serves this file at /downloads/TitanOS.apk

Alternatively, set VITE_TITANOS_APK_URL in .env.local to point at a hosted APK URL.
